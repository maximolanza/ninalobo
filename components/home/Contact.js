import SectionTitle from './SectionTitle';
const Contact = () => {

    return (
        <section >
            <SectionTitle id="contact" title="Contacto" />
        </section>
    );
}

export default Contact;