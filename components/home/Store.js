import SectionTitle from './SectionTitle';
const Store = () => {

    return (
        <section>
            <SectionTitle id="store" title="Store" />
        </section>
    );
}

export default Store;